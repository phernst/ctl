#ifndef CTL_DEN_H
#define CTL_DEN_H

// header that includes everything from the `CTL::io::den` namespace
// + CTL::io::DenFileIO template class

#include "den_utils.h"
#include "denfileio.h"

#endif // CTL_DEN_H
